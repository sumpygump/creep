#!/usr/bin/env python

import sys # System specific parameters and functions

from CreepClient import CreepClient

if __name__ == "__main__":
    client = CreepClient()

    if len(sys.argv) > 1:
        status = client.onecmd(' '.join(sys.argv[1:]))

    sys.exit(status)

